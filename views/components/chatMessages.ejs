<section id="messages">
  <% if (data.messages != undefined) { %> <% for (let i = 0; i <
  data.messages.length; i++) { %> <% if (data.messages[i].sender ===
  data.clientid) { %>
  <div class="message messageSend">
    <p>
      <%= data.messages[i].content %>
    </p>
  </div>

  <% } else { %>

  <div class="message messageRecieve">
    <p>
      <%= data.messages[i].content %>
    </p>
  </div>

  <% } %> <% } %> <% } else { %>
  <div class="message messageRecieve">
    <p>No messages found, start chatting and matching 🦀</p>
  </div>

  <div class="message messageSend">
    <p>No messages found, start chatting and matching 🦀</p>
  </div>
  <% } %>
</section>
